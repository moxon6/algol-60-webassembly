begin

    comment library5;

    integer procedure none;
        none := 1;

    none;
    sin(1.0);
    abs(3);
    ln(.2);
    exp(4);
    entier(none);
    sign(sqrt(8));
    outreal(1, none)
end
