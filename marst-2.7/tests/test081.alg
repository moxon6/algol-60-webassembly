begin

    comment library 1;

    array
        v,
        a,
        c[1 : 1];
    integer
        k;
    integer procedure i;
        begin
        i := 1;
        k := k + 1
        end;

    k := 0;
    a[i] := 1;
    c[i] := 3;
    for v[i] := a[i] step a[i] until c[i] do
        ;
    outreal(1, k)
end
